spring:
  datasource:
    url: jdbc:h2:mem:arena_test_db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop # Șterge tot la finalul testelor (pentru a fi mereu "curat")
    database-platform: org.hibernate.dialect.H2Dialect