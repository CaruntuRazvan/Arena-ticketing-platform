spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/arena-ticketing-dev
    username: postgres
    password: Costica80
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true